package com.example.firebasenotes.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA LIGHT (LIMPIA Y MODERNA) ---

// Azul vibrante pero profesional (tipo Google)
val Primary = Color(0xFF2563EB)
val Secondary = Color(0xFF3B82F6)
val Tertiary = Color(0xFF64748B)

// FONDOS CLAROS
val BackgroundLight = Color(0xFFF1F5F9) // Un gris casi blanco (Slate 100), muy descansado
val SurfaceLight = Color(0xFFFFFFFF)    // Blanco puro para las tarjetas
val SurfaceVariantLight = Color(0xFFE2E8F0) // Gris claro para bordes o inputs

// TEXTOS OSCUROS (Para leer bien sobre blanco)
val OnPrimary = Color.White
val OnBackground = Color(0xFF1E293B) // Gris muy oscuro (Casi negro, pero más suave)
val OnSurface = Color(0xFF0F172A)
val OnSurfaceVariant = Color(0xFF64748B) // Texto secundario (gris medio)

val ErrorColor = Color(0xFFEF4444)