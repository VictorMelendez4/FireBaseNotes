package com.example.firebasenotes.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA SOFT PREMIUM (LIGHT) ---

// AZUL ÍNDIGO (Más elegante que el azul rey)
val Primary = Color(0xFF4F46E5)
val Secondary = Color(0xFF818CF8)
val Tertiary = Color(0xFF3730A3)

// FONDOS: La clave de la calidad
val BackgroundLight = Color(0xFFF2F4F7) // GRIS NUBE (Hace que lo blanco resalte)
val SurfaceLight = Color(0xFFFFFFFF)    // BLANCO PURO (Para tarjetas y buscador)
val SurfaceVariantLight = Color(0xFFE5E7EB) // Bordes sutiles

// TEXTOS: Gris carbón (mejor que negro puro)
val OnPrimary = Color.White
val OnBackground = Color(0xFF111827)
val OnSurface = Color(0xFF1F2937)
val OnSurfaceVariant = Color(0xFF6B7280) // Texto secundario

val ErrorColor = Color(0xFFEF4444)